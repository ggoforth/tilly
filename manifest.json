{
  "manifest_version": 3,
  "name": "Tilly — AI for Meeples",
  "version": "0.2.0",
  "description": "AI for Meeples: Tilly is an Agricola coach for Board Game Arena. She watches the game and streams move advice into a docked panel via your own OpenRouter LLM key. Observe-only — she never plays for you.",
  "permissions": ["scripting", "storage", "unlimitedStorage", "tabs", "activeTab"],
  "host_permissions": ["*://*.boardgamearena.com/*", "https://openrouter.ai/*"],
  "background": {
    "service_worker": "worker.js"
  },
  "options_page": "options.html",
  "content_scripts": [
    {
      "matches": ["*://*.boardgamearena.com/*"],
      "js": ["content.js"],
      "run_at": "document_idle"
    }
  ]
}
