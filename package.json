{
  "name": "tilly",
  "version": "0.1.0",
  "private": true,
  "description": "AI for Meeples — Tilly is an Agricola coach for Board Game Arena. Streams move advice via your own OpenRouter LLM key. Observe-only.",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "typecheck": "tsc --noEmit",
    "test": "node test.mjs",
    "check": "npm run typecheck && npm run test && npm run build"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.270",
    "@types/node": "^25.8.0",
    "esbuild": "^0.23.0",
    "typescript": "^5.5.4"
  }
}
